#!/bin/bash

#-------------------------------------------------------------
# 定义数组 数组名=(值1 值2 值3 值4 值5) || 数组名[index]=值
#-------------------------------------------------------------
array_name=(value1 value2 value3 value4)
array_name[0]=value1
array_name[1]=value2

#------------------------------------------------------------
# 读取数组
#-----------------------------------------------------------
${array_name[index]}
my_array=(A B "C" D)
echo "第一个元素为: ${my_array[0]}"
echo "第二个元素为: ${my_array[1]}"
echo "第三个元素为: ${my_array[2]}"
echo "第四个元素为: ${my_array[3]}"

#-----------------------------------------------------------
# 获取数组中元素
#----------------------------------------------------------
my_array=(a b c d)
echo "数组的元素为:${my_array[@]}"
echo "数组的元素为:${my_array[*]}"

#-----------------------------------------------------------
# 获取数组长度
#-----------------------------------------------------------
len=(a b c d e)
echo ${#len[@]}
echo ${#len[*]}
